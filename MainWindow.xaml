<Window x:Class="Huey.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Huey - Color Picker" Height="400" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="Huey" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Color Picker" FontSize="14" Foreground="Gray" HorizontalAlignment="Center"/>
        </StackPanel>
        
        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Color Preview -->
            <Border Grid.Column="0" 
                    Background="LightGray" 
                    CornerRadius="8" 
                    Margin="0,0,10,0"
                    x:Name="ColorPreview">
                <TextBlock Text="No color selected" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Foreground="Gray"/>
            </Border>
            
            <!-- Colour Values -->
            <StackPanel Grid.Column="2" Margin="10,0,0,0">
                <TextBlock Text="RGB:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock x:Name="RgbValue" Text="0, 0, 0" Margin="0,0,0,10"/>
                
                <TextBlock Text="HEX:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock x:Name="HexValue" Text="#000000" Margin="0,0,0,10"/>
                
                <Button x:Name="CopyButton" 
                        Content="Copy HEX" 
                        Padding="10,5" 
                        Margin="0,10,0,0"
                        Click="CopyButton_Click"/>
            </StackPanel>
        </Grid>
        
        <!-- Footer -->
        <StackPanel Grid.Row="2" Margin="20,10,20,20">
            <Button x:Name="PickColourButton" 
                    Content="Pick Colour from Screen" 
                    Padding="20,10" 
                    FontSize="14"
                    Click="PickColourButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 